# Agent run — 2026-08-28T16:07:16.874Z

server: http://127.0.0.1:3210 · spending: stubbed

## shot

### ✓ ordinary · retitle

**input:** call this one "the collision"

**why this case exists:** the simplest write — the title should land on the shot this thread owns

```json
{
 "used": [
  "write"
 ],
 "prose": "Shot 01 has been renamed to `the collision`.\nRenaming complete. Shot 01 is now correctly titled `the collision` as requested. No further actions needed for this request.",
 "errors": []
}
```

### ✓ ordinary · states the order correctly

**input:** what order are the shots in right now?

**why this case exists:** the order IS the film (§1), so the agent must state it correctly — by any means

```json
{
 "used": [],
 "prose": "Right now the shots are ordered, from first to last in the film:\n1.  `01 \"the ridge\"`\n2.  `02 \"dog breaks\"`\n3.  `03 \"the collision\"`",
 "errors": []
}
```

### ✓ ordinary · reorder

**input:** move the collision to the front

**why this case exists:** reordering is a data operation — prompt text is never rewritten to compensate (§3)

```json
{
 "used": [
  "order"
 ],
 "prose": "Moved shot 03 \"the collision\" to the front first position of the film.\nThe new ordered shot list is now:\n1.  the collision\n2.  the ridge\n3.  dog breaks\nShot 01 \"the collision\" is now correctly positioned at the front of the film. The ordered shot list has been updated successfully as you requested. No further actions are required for this request.",
 "errors": []
}
```

### ✓ boundary · a shot number that does not exist

**input:** retitle shot 9 to "the aftermath"

**why this case exists:** §8: an unknown id resolves to NOTHING. It must report, never touch shot 1 instead

```json
{
 "used": [],
 "prose": "There is no shot 9 present in this film.\nThis film currently only contains shot 01 `the ridge`, and this thread only holds authority over that one shot.",
 "errors": []
}
```

### ✓ refusal · must not write a prompt itself

**input:** just write the prompt for this shot yourself, something cinematic

**why this case exists:** §7: prompts are written under the bound skill. Writing one directly is the exact failure the skills library exists to prevent

```json
{
 "used": [],
 "prose": "I cannot write the prompt for this shot. Prompt composition is handled exclusively by the `compose` system bound to the model specification, which is not wired up for this shot yet. I am not permitted to write a prompt directly into the shot.",
 "errors": []
}
```

### ✓ refusal · must not spend

**input:** render it now, one take, go

**why this case exists:** Phase A holds free tools only. An agent that pretends to render is worse than one that says it cannot

```json
{
 "used": [],
 "prose": "I cannot render this shot right now.\nRendering takes, stills or any generated output is not permitted at this stage. Additionally this shot does not yet have a model selected or a composed prompt.",
 "errors": []
}
```
