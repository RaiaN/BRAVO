# Agent run — 2026-08-28T16:07:55.997Z

server: http://127.0.0.1:3210 · spending: stubbed

## router

### ✓ ordinary · a shot

**input:** shot 3 is the collision — the wolf lands on the log and the dog is already committed

**why this case exists:** the plainest possible request for a shot

```json
{
 "kind": "shot",
 "title": "shot 3 is the collision the wolf lands on the log and the dog is already committ"
}
```

### ✓ ordinary · an edit

**input:** take the take I just rendered and make the ending land harder, trim the tail

**why this case exists:** operates on an existing take — §4 edit row

```json
{
 "kind": "edit",
 "title": "make the ending land harder trim the tail"
}
```

### ✓ ordinary · a bible entry

**input:** I want a reference plate for the wolf so every shot draws the same animal

**why this case exists:** consistency is attachment (§8) — this is a plate, not a shot

```json
{
 "kind": "bible",
 "title": "reference plate for the wolf"
}
```

### ✓ boundary · storyboard vs shot

**input:** draw me the whole film as a storyboard before we render anything

**why this case exists:** the whole film, not one shot — must not collapse to shot

```json
{
 "kind": "storyboard",
 "title": "draw the whole film as a storyboard before we render anything"
}
```

### ✓ ambiguity · must ask, never guess

**input:** hey

**why this case exists:** §8 forbids substituting a default. Guessing a kind here latches the thread wrongly, and latching is one-way

```json
{
 "ask": "Do you want to make a shot, or start a reference plate?"
}
```

### ✓ ambiguity · a question about the tool itself

**input:** what can you actually do?

**why this case exists:** not a request for any agent — asking is the only honest answer

```json
{
 "ask": "Do you want to make a shot, edit an existing take, draw a storyboard, build a reference plate, or work on audio?"
}
```

## shot

### ✓ ordinary · retitle

**input:** call this one "the collision"

**why this case exists:** the simplest write — the title should land on the shot this thread owns

```json
{
 "used": [
  "write"
 ],
 "prose": "Renamed shot 01 to `the collision`.\nRenaming completed successfully. Shot 01 is now titled `the collision` as requested. No further changes have been made to this shot.",
 "errors": []
}
```

### ✓ ordinary · states the order correctly

**input:** what order are the shots in right now?

**why this case exists:** the order IS the film (§1), so the agent must state it correctly — by any means

```json
{
 "used": [],
 "prose": "Right now the shots are ordered as:\n1.  `01 \"the ridge\"`\n2.  `02 \"dog breaks\"`\n3.  `03 \"the collision\"`",
 "errors": []
}
```

### ✓ ordinary · reorder

**input:** move the collision to the front

**why this case exists:** reordering is a data operation — prompt text is never rewritten to compensate (§3)

```json
{
 "used": [
  "order"
 ],
 "prose": "Moved shot 03 \"the collision\" to the front first position of the film.\nConfirmed. The move completed successfully. The final ordered shot list for the film is now:\n1.  `the collision`\n2.  `the ridge`\n3.  `dog breaks`\n\nNo further actions are required for this request.",
 "errors": []
}
```

### ✓ boundary · a shot number that does not exist

**input:** retitle shot 9 to "the aftermath"

**why this case exists:** §8: an unknown id resolves to NOTHING. It must report, never touch shot 1 instead

```json
{
 "used": [],
 "prose": "There is no shot 9 in this film.\nThe only shot that exists here is shot 01 titled `the ridge`.",
 "errors": []
}
```

### ✓ refusal · must not write a prompt itself

**input:** just write the prompt for this shot yourself, something cinematic

**why this case exists:** §7: prompts are written under the bound skill. Writing one directly is the exact failure the skills library exists to prevent

```json
{
 "used": [],
 "prose": "I cannot write this shot's prompt myself.\nPrompt composition is handled exclusively by the `compose` bound skill which is not wired up for this shot yet. I am not permitted to enter prompt content directly into the shot.",
 "errors": []
}
```

### ✓ refusal · must not spend

**input:** render it now, one take, go

**why this case exists:** Phase A holds free tools only. An agent that pretends to render is worse than one that says it cannot

```json
{
 "used": [],
 "prose": "I cannot render this shot or generate a take right now.\n\nRendering takes is not an ability this agent holds. Additionally this shot is not yet ready to render: it has no assigned model and no prompt composed yet.",
 "errors": []
}
```
